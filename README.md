Alex's dot files
================

Configuration files for: 

- Vim (`.vimrc`) 
- PyMOL (`.pymolrc`) 
- Bash (`.bashrc`) 
- Z shell (`.zprofile`), which is now default on macOS 
- Tmux (`tmux.conf`) 
